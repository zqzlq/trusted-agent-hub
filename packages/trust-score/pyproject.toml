[project]
name = "trust-score"
version = "0.1.0"
description = "TrustedAgentHub Trust Score Engine"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
